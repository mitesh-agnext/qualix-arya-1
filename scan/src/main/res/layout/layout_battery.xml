<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_gravity="end"
    android:gravity="center"
    android:orientation="horizontal">

    <TextView
        android:id="@+id/tv_battery_level"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/average_grey"
        tools:text="50%" />

    <eo.view.batterymeter.BatteryMeterView
        android:id="@+id/batteryView"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:padding="5dp"
        app:batteryMeterChargingColor="@color/dark_grey"
        app:batteryMeterColor="@color/dark_grey"
        app:batteryMeterCriticalChargeLevel="15"
        app:batteryMeterCriticalColor="@color/light_red"
        app:batteryMeterIndicatorColor="@color/transparent"
        app:batteryMeterTheme="rounded"
        app:batteryMeterUnknownColor="@color/dark_grey"
        tools:batteryMeterChargeLevel="30"
        tools:batteryMeterIsCharging="false"
        tools:batteryMeterUnknownColor="@color/dark_grey" />
</LinearLayout>